import React, { Component } from 'react'

export default class ProductCard extends Component {
    render(props) {
        return (
            <div>
        Personajess{console.log(this.props.handleOnSubmit)}
            </div>
        )
    }
}
